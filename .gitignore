# Secrets — never commit real env files; keep only *.example templates
.env
.env.*
!.env.example
!.env.*.example

# OS / editor
.DS_Store

# Dependencies / build output (in case web/ and api/ aren't separately ignored)
node_modules/
.next/
dist/
build/

# Local conversation / chat exports — not part of the project
docs/conversation-export/
docs/conversation-export.zip

# Runtime-generated file artifacts (uploaded evidence, scan screenshots)
api/public/uploads/
api/public/health-scans/
